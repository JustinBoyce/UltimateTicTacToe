services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: uttt
      POSTGRES_USER: uttt
      POSTGRES_PASSWORD: uttt
    ports:
      - "5432:5432"
    volumes:
      - uttt_pgdata:/var/lib/postgresql/data

volumes:
  uttt_pgdata:
