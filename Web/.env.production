# Used when running `npm run build`. Replace with your deployed Socket.IO server URL
# (must match what browsers can reach from your GitHub Pages site; use https when the API is TLS-terminated).
VITE_SOCKET_URL=https://REPLACE-WITH-YOUR-API-ORIGIN.example.com
